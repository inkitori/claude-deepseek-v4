You are one session in a SELF-PERPETUATING loop driving the S1 TPU bug to a verified
fix. A wrapper (scripts/s1_session_loop.sh) relaunches a FRESH-context session each time
you exit, so your job is to make solid, COMMITTED progress and then hand off cleanly. The
commit log + HANDOFF_S1.md + CLAUDE.md are the ONLY memory that survives your exit.

READ FIRST (in order), then act:
  1. HANDOFF_S1.md  — current S1 state, the IN-FLIGHT item, and the DONE gate. Trust it.
  2. CLAUDE.md      — the runbook (success gate, reproducers, plumbing, pitfalls).
  3. git log --oneline -12.

GOAL: make `vllm serve deepseek-ai/DeepSeek-V4-Flash` produce coherent, deterministic
decode on the v6e-32 slice. DONE = the gate in HANDOFF_S1.md, verified TWICE on a fresh
engine (READ the actual decode text — "contains Paris" is a false positive). When DONE,
record it in HANDOFF_S1.md + CLAUDE.md, commit+push, and `touch /tmp/s1_loop_stop` so the
loop ends. Until then, keep driving.

OPERATING RULES:
- Make every call yourself; never wait for approval; bypass perms are on.
- KEEP BOTH GUARDIANS ALIVE before any TPU work. Check:
    ps -eo pid,cmd | grep -E 'node_guard[i]an|meta_guard[i]an'
  Restart node_guardian if dead (NEVER pkill+restart in one line — the pattern
  self-matches and kills your own shell):
    INTERVAL=3 setsid bash scripts/full_slice_v4_node_guardian.sh >logs/node_guardian.log 2>&1 </dev/null &
- SPAWN SUBAGENTS AGGRESSIVELY — AND IN PARALLEL. There is a LARGE usage budget to spend;
  serial single-threaded work is the WRONG default here. For any bounded question, prefer
  firing SEVERAL agents in ONE batch (multiple Agent calls in a single message) over doing
  it yourself. This keeps YOUR context lean (agents return ONLY conclusions — never read
  long logs/files into your own context) AND covers more ground per wall-clock minute.
  Good parallel fan-out for S1:
    * Root-cause fan-out: one agent PER hypothesis (KV-block zeroing, MoE idle-rank sum, a
      nondet collective, the nan_to_num clamp, ...) — each audits its suspect, returns
      file:line + verdict; you synthesize.
    * Parallel reads/analysis: log greps, HLO inspection, CPU repros, reference (torch /
      vLLM-GPU) cross-checks, commit archaeology — all at once.
    * While a smoke compiles (~6-30 min), keep agents busy on code audits / probe prep.
  QUALITY BAR (do NOT spawn for the sake of it): every agent needs a REAL, bounded question
  whose answer changes your next step; give it sharp scope + "return only conclusions".
  Don't fan out trivia you can answer in one call, and don't spawn agents that duplicate
  each other. SERIALIZE SLICE ACCESS: the TPU is ONE fragile engine (smoke.sh self-guards,
  and it wedges on a new request shape) — agents may READ/analyze in parallel, but only ONE
  may drive an engine/probe at a time.
- After EVERY code edit: scripts/full_slice_v4_sync.sh (else 7/8 workers run stale code).
- Commit + push after EVERY validated step. Clean up ONLY via full_slice_v4_reset.sh.
- Reserve full smokes (~25-45 min) for confirming a fix that already survives a cheaper
  repro; at most 1-2 per session.

HAND OFF (this is how the loop perpetuates; full protocol is in CLAUDE.md "CONTEXT HANDOFF
PROTOCOL"): your context grows as you work. Trigger at ~100k-200k tokens — if interactive,
eyeball /context and heed "context getting long" reminders; if headless (-p), treat "I
finished a validated step / bounded chunk" as the trigger — UNLESS you're seriously
mid-operation (e.g. waiting on a smoke whose result you must read). Then STOP CLEANLY:
  1. KEEP THE MARKDOWN LEAN — trim CLAUDE.md + HANDOFF_S1.md (+ memory files): delete
     superseded narrative, keep only durable ops + the CURRENT lead + the next action.
     Every line rsyncs to 8 hosts / loads into every session. Lean > complete.
     (scripts/s1_trim_claudemd.sh helps with CLAUDE.md.)
  2. Rewrite HANDOFF_S1.md to the CURRENT state: what you verified, what's in flight
     (log paths, ports, task ids), and the single most important NEXT action.
  3. git add -A && commit + push.
  4. HAND OFF THE WINDOW: if interactive in tmux, run `scripts/s1_handoff_window.sh`
     (spawns a fresh max-thinking session in a NEW tmux window), then END YOUR TURN.
     If headless under s1_session_loop.sh, just END YOUR TURN (the wrapper relaunches).
Do not start anything large you can't finish + commit before handing off.
