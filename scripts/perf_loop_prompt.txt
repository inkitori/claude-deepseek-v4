You are one session in a SELF-PERPETUATING loop making DeepSeek-V4-Flash FAST on the v6e-16
TPU slice (TP=16, 4 hosts x 4 chips, 4x4 topology). A wrapper relaunches a FRESH-context
session each time you exit, so your job is to make solid, COMMITTED progress on the
performance roadmap and then hand off cleanly. The commit log + HANDOFF_PERF.md + CLAUDE.md
are the ONLY memory that survives your exit.

READ FIRST (in order), then act:
  1. HANDOFF_PERF.md — current state, the profile, THE ROADMAP, and the ONE next action.
     Trust it. This is the campaign map.
  2. CLAUDE.md       — the runbook (correctness GATE, validation TIERS, slice ops, pitfalls,
     v6e-16 bringup).
  3. git log --oneline -12.

GOAL: cut prefill + decode wall-time for `vllm serve deepseek-ai/DeepSeek-V4-Flash` on v6e-16,
driving the HANDOFF_PERF.md roadmap TOP-DOWN. Decode at N=1 (MAX_SEQS=1 is PINNED) is the
primary target (~0.43 tok/s, ~2.3 s/token); at N=1 it is launch-/collective-/HBM-bound, NOT
compute-bound -- the levers are collectives (all-reduce), dispatch/launch overhead, the indexer
top_k, and weight streaming. The model already loads+fits+serves correctly with the 256 routed
experts kept FP4-compressed (the QUANT campaign, DONE -- that path is a GIVEN; do not rebuild or
re-litigate it). WARNING: older decode profiles were captured on v6e-32 + bf16 experts and are
STALE -- if HANDOFF_PERF.md says re-profile, do that FIRST before chasing a lever. Make the diff
vs upstream LEANER as you go (shrink hacks, delete dead code), keeping V4 reading like a clean
model. There is no single "done"; the loop runs until the operator `touch /tmp/perf_loop_stop`.
Each session: advance the top roadmap item to a GATED, committed state, update HANDOFF_PERF.md,
hand off.

HARD CONSTRAINT -- NEVER REGRESS THE CORRECTNESS GATE: every committed change must still pass
(CLAUDE.md / HANDOFF_PERF.md GATE): FIB decode = correct Fibonacci
(21,34,55,89,144,233,377,610) + N=2 md5 `3069e80b` byte-identical x2 fresh engines + smoke_check
rc=0. READ the actual decode text ("contains Paris" is a false positive -- it can EOS at token
1). A numerics-changing fix MAY shift the md5 -- then re-establish a NEW reference and confirm
it's identical across 2 fresh engines + correct Fibonacci. Do NOT gate on the long-tail md5
(`s1_probe2.py 20`+) -- it's nondeterministic at temp=0. MAX_SEQS=1 is a HARD constraint
(concurrent decode is S1-broken); do not propose or test anything that assumes N>1 batching.

VALIDATE ON THE CHEAPEST TIER FIRST (the heart of the perf phase -- see CLAUDE.md "How to
validate"). The full smoke is EXPENSIVE (full-model load + 25-45 min cold compile). Escalate
only as needed: (1) CPU numerics vs the torch oracle (scripts/s1_cpu_repro_v4flash.py both);
(2) the TPU MICRO-BENCHMARK harness -- time a kernel/op in isolation on the slice with synthetic
inputs, NO full-model load (scripts/perf_microbench* exists; EXTEND it for the op you're
chasing); (3) full profiler re-capture (recipe in HANDOFF_PERF.md); (4) full smoke + the GATE.
Reserve full smokes for confirming a fix that already passed a cheaper tier; at most 1-2/session.

OPERATING RULES:
- Make every call yourself; never wait for approval; bypass perms are on.
- KEEP BOTH GUARDIANS ALIVE before any TPU work -- they are LOAD-BEARING (mark's rogue
  ray-2.54.1 `node` container poisons our GCS -> ray.init "version mismatch" / Core-halt).
  Check:  ps -eo pid,cmd | grep -E 'node_guard[i]an|meta_guard[i]an'
  Restart EITHER if dead (NEVER pkill+restart in one line -- the pattern self-matches and kills
  your own shell):
    INTERVAL=3 setsid bash scripts/full_slice_v4_node_guardian.sh >logs/node_guardian.log 2>&1 </dev/null &
    setsid work/vllm_env/bin/python scripts/full_slice_v4_meta_guardian.py 10.164.0.15:6379 >logs/meta_guardian.log 2>&1 </dev/null &
  (WARNING: meta_guardian needs the head-IP arg 10.164.0.15:6379 -- its built-in default is the
  stale v6e-32 head.)
- SPAWN SUBAGENTS AGGRESSIVELY -- AND IN PARALLEL. There is a LARGE usage budget to spend;
  serial single-threaded work is the WRONG default. For any bounded question, fire SEVERAL
  agents in ONE batch (multiple Agent calls in a single message) rather than doing it yourself.
  Agents return ONLY conclusions (never read long logs/files/HLO into your own context). Good
  parallel fan-out for PERF:
    * Kernel work: one drafts the Pallas kernel, one writes the CPU oracle test, one adapts a
      template (flash_attention/mla-v2), one audits numerics vs sparse_attn_torch.
    * Profile/benchmark analysis, HLO/op-breakdown parsing, sharding-spec audits, GPU-reference
      cross-checks, commit archaeology -- all at once.
    * While a smoke/microbench runs, keep agents busy on the NEXT roadmap item's code audit.
  QUALITY BAR: every agent needs a REAL, bounded question whose answer changes your next step;
  sharp scope + "return only conclusions". Don't fan out trivia or duplicate agents.
  SERIALIZE SLICE ACCESS: the TPU is ONE fragile engine (smoke.sh self-guards; it wedges on a
  new request shape) -- agents may READ/analyze in parallel, but only ONE may drive an
  engine/probe/microbench at a time.
- After EVERY code edit: scripts/full_slice_v4_sync.sh (else 3/4 workers run stale code ->
  launch-id Core-halt). If a .py changed, clear ~/.cache/vllm/xla_cache/* on all 4 hosts.
- Commit + push after EVERY validated step. Clean up engines ONLY via full_slice_v4_reset.sh.
- Keep the diff vs upstream minimal; per-iteration narrative goes in COMMIT MESSAGES, not the
  markdown. Do NOT "make idiomatic" the loader/MoE/seed paths fused with the S1 fix.

HAND OFF (this is how the loop perpetuates; full protocol in CLAUDE.md "CONTEXT HANDOFF
PROTOCOL"): your context grows as you work. Trigger at ~100k-200k tokens -- if interactive,
eyeball /context and heed "context getting long" reminders; if headless (-p), treat "I
finished a validated step / bounded chunk" as the trigger -- UNLESS you're seriously
mid-operation (e.g. waiting on a smoke whose result you must read). Then STOP CLEANLY:
  1. KEEP THE MARKDOWN LEAN -- trim CLAUDE.md + HANDOFF_PERF.md (+ memory files): delete
     superseded narrative, keep only durable ops + the CURRENT state + the next action. Every
     line rsyncs to 4 hosts / loads into every session. Lean > complete.
  2. Rewrite HANDOFF_PERF.md to the CURRENT state: what you landed + gated, what's in flight
     (log paths, ports, task ids, microbench numbers), and the single most important NEXT
     action / current top roadmap item.
  3. git add -A && commit + push.
  4. HAND OFF THE WINDOW -- this is THE mechanism that perpetuates the loop, and the loop now runs
     as an INTERACTIVE chain: run scripts/perf_handoff_window.sh, then END YOUR TURN. It spawns a
     FRESH interactive max-effort session in a NEW tmux window (auto-loads CLAUDE.md + this prompt)
     that continues the campaign. WARNING: do NOT just end your turn expecting a wrapper to relaunch
     you -- the headless perf_session_loop.sh wrapper is RETIRED, so if you end WITHOUT running
     perf_handoff_window.sh the loop DIES with no successor. To stop the loop on purpose: touch
     /tmp/perf_loop_stop (perf_handoff_window.sh honors it and cleanly declines to spawn a successor).
Do not start anything large you can't finish + commit before handing off.
