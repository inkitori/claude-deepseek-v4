# SUMMARY.md (archived)

This file held a multi-version (v1–v8) summary of the autonomous
overnight sessions that built the V4 implementation. The narrative
is preserved in `git log`.

For the current high-level overview, read:

* [`../../README.md`](../../README.md) — what this repo is + how to
  bring up a fresh slice.
* [`../../CLAUDE.md`](../../CLAUDE.md) — operational runbook + the
  minimum-delta rule.

If you find yourself wanting to summarize per-session state in a
file like this during a future agent iteration, **don't** — the
commit message + CLAUDE.md updates are the right surface.
