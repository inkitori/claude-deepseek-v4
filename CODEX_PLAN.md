# CODEX_PLAN.md (archived)

This file held a partial plan + load-time / KV-cache budget analysis
from a prior Codex session. Its operational content has been
superseded by **[CLAUDE.md](CLAUDE.md)**, which is now the
authoritative runbook.

The file is retained as an empty stub so historical references
(commit messages, prior session memory) still resolve. New runbook
content should land in CLAUDE.md.
